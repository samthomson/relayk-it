/**
 * Entry script for pages that show the cube/hero: mounts the Rubik's cube
 * (any `[data-rubix-cube]` canvases) and the typewriter title (no-ops if absent).
 */
import './rubix-cube.js';
import './typewriter.js';
